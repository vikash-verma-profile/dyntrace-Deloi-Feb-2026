sudo mysql

CREATE DATABASE ecommerce;
USE ecommerce;

CREATE TABLE orders (
    id INT AUTO_INCREMENT PRIMARY KEY,
    product VARCHAR(100),
    quantity INT,
    status VARCHAR(50)
);

INSERT INTO orders (product, quantity, status)
VALUES ('Laptop', 1, 'NEW'),
       ('Mobile', 2, 'NEW'),
       ('Headphones', 1, 'NEW');

CREATE USER 'ecommerceuser'@'localhost' IDENTIFIED BY 'password';
GRANT ALL PRIVILEGES ON ecommerce.* TO 'ecommerceuser'@'localhost';
FLUSH PRIVILEGES;
EXIT;

mkdir dynatrace-davis-lab
cd dynatrace-davis-lab
npm init -y
npm install express mysql2
nano server.js
node server.js


PUT http://localhost:3000/orders/1/slow

for i in {1..5000}; do curl http://localhost:3000/orders & done